import Typed from 'typed.js';

var options = {
  strings: ["I'm a Full-Stack Developer", 'I Build  Websites', 'I Make Ideas into Projects'],
  typeSpeed: 160,
  backSpeed: 50,
  backDelay: 1500
  // loop: true
};

var typed = new Typed('#headline', options);
